# Написать манифест для запуска приложения в Kubernetes:
# 
# [o] В отдельном неймспейсе в виде Deployment
# [o] C 3 репликами
# [o] Cервисом типа ClusterIP (default type)
# [o] Реализовать readiness- и liveness- пробы. 
# [o] В переменную UUID должен подставляться уникальный идентификатор пода в кластере.

apiVersion: v1
kind: Namespace
metadata:
  name: http-app-namespace
---
apiVersion: v1
kind: Service
metadata:
  name: http-app-service
  namespace: http-app-namespace
spec:
  selector:
    app: http-app
  ports:
  - name: http
    port: 8000
    targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-app
  namespace: http-app-namespace
spec:
  replicas: 3
  selector:
    matchLabels:
      app: http-app
  template:
    metadata:
      labels:
        app: http-app
    spec:
      containers:
      - name: http-app
        image: http-app-img
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: UUID
          valueFrom:
          fieldRef:
            fieldPath: metadata.uid
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 20